---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart offsite timer
  ansible.builtin.systemd:
    name: offsite-backup.timer
    state: restarted
    enabled: true
    daemon_reload: true
