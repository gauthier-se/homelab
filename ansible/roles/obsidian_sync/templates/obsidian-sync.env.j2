# {{ ansible_managed }}
# Read by Docker Compose for `${...}` interpolation in compose.yaml. Every
# value comes from OpenBao at play time.
ADMIN_USER={{ obsidian_sync_admin_user }}
ADMIN_PASSWORD={{ obsidian_sync_admin_password }}
