# {{ ansible_managed }}
# Kept out of the unit file on purpose: unit files are world-readable and
# `systemctl show` prints Environment= values back to any user.
CF_DNS_API_TOKEN={{ traefik_cloudflare_token }}
